String in Scala are arrays of 16-bit Chars
Tuples are a collection of values that can be of different types. It can be 1 to 22 items long. Tuples are usually pretty small, so if they are getting larger and more complex (more fields can get mixed up since you call items in tuples by number. Consider using a case class or defining a class